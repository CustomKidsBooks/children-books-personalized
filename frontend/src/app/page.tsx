import LandingPage from "@components/LandingPage";

const Home = () => {
  return (
    <div>
      <section className="w-full bg-green">
        <LandingPage />
      </section>
    </div>
  );
};

export default Home;
